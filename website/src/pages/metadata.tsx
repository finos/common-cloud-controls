import React from "react";
import Layout from "@theme/Layout";

export default function MetadataPage(): React.ReactElement {
  return (
    <Layout title="Metadata">
      <main />
    </Layout>
  );
}
