loglevel: info
write-directory: evaluation_results
write: true
output: yaml

# Good fixture: no public function URL.

services:
  awsServerlessGood:
    plugin: ccc-behavioural-plugin
    policy:
      catalogs:
        - CCC.SvlsComp
      applicability:
        - tlp-amber
        - tlp-red
    vars:
      service: serverless-computing
      ServiceType: serverless-computing
      tags: "@Behavioural @serverless-computing"
      provider: aws
      region: us-east-1
      resource: "cfi-20260527t120000z-fn-good"
      function-name: "cfi-20260527t120000z-fn-good"
      private-endpoint-url: "https://private-fn-good.internal.example.com/invoke"
      public-invoke-url: ""
      rate-limit-threshold: 10
      burst-overrun: 15
      permitted-regions:
        - us-east-1
      aws-flow-log-group-name: "/aws/lambda/cfi-20260527t120000z-fn-good"
      test-identities:
        testUserNoAccess:
          user-name: "${AWS_TEST_USER_NO_ACCESS_USER_NAME}"
          access_key_id: "${AWS_TEST_USER_NO_ACCESS_ACCESS_KEY_ID}"
          secret_access_key: "${AWS_TEST_USER_NO_ACCESS_SECRET_ACCESS_KEY}"
          session_token: "${AWS_TEST_USER_NO_ACCESS_SESSION_TOKEN}"
