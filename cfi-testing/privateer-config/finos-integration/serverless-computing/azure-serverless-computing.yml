loglevel: info
write-directory: evaluation_results
write: true
output: yaml

services:
  azureServerlessComputing:
    plugin: ccc-behavioural-plugin
    policy:
      catalogs:
        - CCC.SvlsComp
      applicability:
        - tlp-amber
        - tlp-red
    vars:
      service: serverless-computing
      service-type: serverless-computing
      tags: "@Behavioural @serverless-computing"
      provider: azure
      catalog-versions:
        CCC.Core: v2025.10
        CCC.SvlsComp: DEV
      region: eastus
      resource: "finos-ccc-integration-fn-main"
      function-name: "finos-ccc-integration-fn-main"
      azure-subscription-id: "${AZURE_SUBSCRIPTION_ID}"
      azure-resource-group: "finos-ccc-integration-rg"
      private-endpoint-url: "https://finos-ccc-integration-fn-main.privatelink.azurewebsites.net/api/HttpTrigger"
      public-invoke-url: ""
      rate-limit-threshold: 10
      burst-overrun: 15
      permitted-regions:
        - eastus
      azure-log-analytics-workspace-id: "${AZURE_LOG_ANALYTICS_WORKSPACE_ID}"
      test-identities:
        test-user-no-access:
          user-name: "${AZURE_TEST_USER_NO_ACCESS_USER_NAME}"
          client_id: "${AZURE_TEST_USER_NO_ACCESS_CLIENT_ID}"
          client_secret: "${AZURE_TEST_USER_NO_ACCESS_CLIENT_SECRET}"
          object_id: "${AZURE_TEST_USER_NO_ACCESS_OBJECT_ID}"
