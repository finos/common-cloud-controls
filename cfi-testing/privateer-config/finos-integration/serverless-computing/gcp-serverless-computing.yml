loglevel: info
write-directory: evaluation_results
write: true
output: yaml

services:
  gcpServerlessComputing:
    plugin: ccc-behavioural-plugin
    policy:
      catalogs:
        - CCC.SvlsComp
      applicability:
        - tlp-amber
        - tlp-red
    vars:
      service: serverless-computing
      ServiceType: serverless-computing
      tags: "@Behavioural @serverless-computing"
      provider: gcp
      catalog-locations:
        CCC.Core: website/src/data/ccc-releases/CCC.Core_v2025.10.yaml
        CCC.SvlsComp: website/src/data/ccc-releases/CCC.SvlsComp_DEV.yaml
      region: us-central1
      gcp-project-id: "${GCP_PROJECT_ID}"
      resource: "finos-ccc-integration-fn-main"
      function-name: "finos-ccc-integration-fn-main"
      private-endpoint-url: "internal-only"
      public-invoke-url: ""
      rate-limit-threshold: 10
      burst-overrun: 15
      permitted-regions:
        - us-central1
      test-identities:
        testUserNoAccess:
          user-name: "${GCP_TEST_USER_NO_ACCESS_NAME}"
          service_account_key: "${GCP_TEST_USER_NO_ACCESS_SA_KEY_JSON}"
